---
import { ScrollArea } from "../components/ui/ScrollArea";
import MainLayout from "./MainLayout.astro";
const { content } = Astro.props;
---

<MainLayout {content}>
  <div class="p-8 md:pl-44">
    <div
      class="grid h-screen grid-flow-row auto-rows-auto justify-evenly gap-5 overflow-auto md:grid md:grid-cols-2 md:overflow-hidden lg:gap-16"
    >
      <div class="h-fit max-h-[300px] overflow-auto md:h-fit md:max-h-fit">
        <div class="md:mb-10 md:pt-12">
          <slot name="title" />
        </div>
        <slot name="description" />
      </div>
      <div class="">
        <div
          class="fixed z-10 h-16 w-full bg-gradient-to-b from-white/[0.9] to-white/[0] dark:from-black/90 dark:to-black/0 md:top-10"
        >
        </div>
        <div
          class="fixed bottom-10 z-10 h-20 w-full bg-gradient-to-t from-white/[0.9] to-white/[0] dark:from-black/90 dark:to-black/0"
        >
        </div>
        <ScrollArea
          client:only="react"
          className="md:h-[calc(100vh-50px)] h-[calc(100vh-308px)] pb-32 "
          classNameForView="flex flex-col gap-5 "
        >
          <slot name="images" />
        </ScrollArea>
      </div>
    </div>
  </div>
</MainLayout>
